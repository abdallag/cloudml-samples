from setuptools import find_packages
from setuptools import setup

setup(
    name='c10train',
    version='0.1',
    install_requires='',
    packages=find_packages(),
    include_package_data=True,
    description='CIFAR10 PyTorch training sample',
)
